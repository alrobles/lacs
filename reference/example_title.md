# Vector with 100 examples of titles from papers. Is used as an example to build an abstracts class.

Vector with 100 examples of titles from papers. Is used as an example to
build an abstracts class.

## Usage

``` r
data(example_title)
```

## Format

An object of class `"character"` with 100 titles

## Examples

``` r
data(example_title)
```
