# Example of plus model fitted with abstracts.

Example of plus model fitted with abstracts.

## Usage

``` r
data(example_plus)
```

## Format

An object of class `"plus"`;

## Examples

``` r
data(example_plus)
```
