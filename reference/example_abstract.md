# Vector with 100 examples of abstracts from papers. This abstracts are used as an example to create a object of class abstracts.

Vector with 100 examples of abstracts from papers. This abstracts are
used as an example to create a object of class abstracts.

## Usage

``` r
data(example_abstract)
```

## Format

An object of class `"character"` with 100 abstracts.

## Examples

``` r
data(example_abstract)
```
