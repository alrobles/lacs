# Example of vocabulary. Is a vocabulary object used to test the new_plus function.

Example of vocabulary. Is a vocabulary object used to test the new_plus
function.

## Usage

``` r
data(example_vocabulary)
```

## Format

An object of class `"text2vec_vocabulary"`;

## Examples

``` r
data(example_vocabulary)
```
