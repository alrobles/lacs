# Vector with 100 examples of DOI from papers. Is used as an example to build an abstracts class.

Vector with 100 examples of DOI from papers. Is used as an example to
build an abstracts class.

## Usage

``` r
data(example_doi)
```

## Format

An object of class `"character"` with 100 DOI.

## Examples

``` r
data(example_doi)
```
